export function hostToOperatorEmailTemplate(area: string): string {
  return `Hi — I'm reaching out via Revolve Co-Hosts after you accepted my intro. I'd love to discuss co-hosting for my property in ${area}. When works for a quick call?`;
}

export function hostToOperatorWhatsAppTemplate(area: string): string {
  return `Hi — we matched on Revolve Co-Hosts. I'm the host who requested an intro for ${area}. Happy to chat whenever you're free.`;
}
